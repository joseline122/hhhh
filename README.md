# ABC E-commerce Next.js Demo

Quick scaffold demonstrating Next.js routing features and Tailwind CSS.

Commands:

```bash
npm install
npm run dev
```

Pages:
- `/` Home with filtering via query params
- `/posts` Posts listing
- `/posts/[id]` Dynamic post page with programmatic navigation
